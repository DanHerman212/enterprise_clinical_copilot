# Pinned to TFDV's last published linux-x86_64 / py3.11 release line.
# TFDV pulls TF, apache-beam, pyarrow, protobuf transitively.
tensorflow-data-validation>=1.16.0,<2.0

# BigQuery access for in-container data load.
google-cloud-bigquery>=3.25.0
google-cloud-bigquery-storage>=2.25.0
google-auth>=2.30.0
db-dtypes>=1.2.0
pyarrow>=16.0.0
pandas>=2.2.0
