# Session — 2026-08-20

_Short, durable record of today's decisions and progress (recovered after a chat
loss; kept in-repo so it survives any future session restart)._

## Decisions made today

- **Agent Evaluation page — kept as-is.** No rename to "Observability" and no
  sibling card. The Agent Eval page already covers the crux of what observability
  surfaced; observability is considered solved for now.
- **Playground / Prompt Management — pinned.** Implement only **after** the
  synthetic cohort is solved (re-validate the golden eval on fabricated data
  before wiring the Langfuse prompt manager into the app).
- **Synthetic cohort (agenda #6)** — the **first item of the launch track**, not a
  pre-public tooling item. It is the **gate** that unblocks GA + launch (#5),
  preview, public launch, and the pinned Playground work.
- **LangGraph graph-view spike (#4) → decided: proper integration, not a shim.**
  The official LangGraph integration requires the modern Langfuse stack; the current
  pins (`langfuse 2.60.10` + `langgraph 1.2.10`) are mutually incompatible for the
  official callback. Decision: **migrate to Langfuse v4 (OTel-native)** + `langgraph
  1.2.11` + `langchain 1.x`, wire the official callback, upgrade the self-hosted
  server, and re-validate the eval gate. Full plan:
  `projects/agent-harness/docs/langfuse_v4_migration_plan.md`.

## Done today

- **#1 Agent Eval page decision** — closed (keep as-is; see above).
- **#2 Quota-refund fix** — `6a16feb`, 40 tests pass (BFF refunds + 502 on
  graceful tool-error payloads that previously returned 200-with-error and
  silently consumed a credit).
- **#3 Phase 6 wrap** — Langfuse build committed, `go_live_plan.md` Phase 5/6
  boxes ticked; **Block B live agent-down verify PASSED**: logged into the demo
  with endpoints down, ran the risk chip → clean error surfaced + **quota refund
  confirmed** (Requests left unchanged). Phase 6 exit criteria met.
- **Langfuse warmup** — 5-stop mental model (Trace / Observations / Generation /
  Span / Scores); system prompt now included in trace inputs (`d005ba2`, live on
  next agent rebuild).

## Pending (remaining agenda)

1. **Langfuse v4 migration (#4, now a proper-integration rebuild)** — migrate to
   Langfuse v4 + `langgraph 1.2.11` + `langchain 1.x`, wire the official LangGraph
   callback, upgrade the self-hosted server, verify the graph view live (needs an
   endpoint redeploy ~38 min), then re-validate the eval gate. Also carries the
   prompt-in-trace-inputs change live.
2. **GA + launch strategy (#5)** — the paid-resources item (budget + tracking to
   connect a launch campaign to traffic / login requests).
3. **Synthetic cohort (#6)** — the multi-day workstream; start after the migration
   + #5 discussions wrap.

## Roadmap shape going forward

**Finish #1 + #4 → synthetic cohort → #5 paid/launch → preview → public → then
Playground / Prompt Mgmt.**

## Reference

- Plan tracker: `danielmherman/docs/go_live_plan.md`
- Prior wrap-up: `projects/agent-harness/docs/phase6_wrapup_2026-08-19.md`
