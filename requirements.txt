# Core data-representation phase only.
# Modeling/serving libraries are deliberately omitted; they will be added
# in their own requirements file when those phases begin.

# --- BigQuery access ---
google-cloud-bigquery>=3.25.0
google-cloud-bigquery-storage>=2.25.0
google-auth>=2.30.0
db-dtypes>=1.2.0          # required for BQ -> pandas with DATE/TIME types
pyarrow>=16.0.0           # BigQuery Storage API + parquet

# --- Notebook / dataframes ---
pandas>=2.2.0
ipykernel>=6.29.0
jupyter>=1.0.0

# --- Clinical baselines (LACE / HOSPITAL evaluation) ---
scikit-learn>=1.5.0       # AUROC / AUPRC for baseline scores
matplotlib>=3.8.0         # ROC / PR / calibration plots logged to Vertex runs

# --- Phase D: data validation (Evidently OSS) ---
evidently>=0.6            # schema, data quality, and drift detection (PSI/KS/chi-square) with HTML reports

# --- Phase E: EDA + feature selection ---
scipy>=1.13               # hierarchical clustering (linkage) for redundancy analysis
lightgbm>=4.3             # multivariate signal baseline for permutation importance
shap>=0.46                # tree-SHAP explanations for the LightGBM baseline
seaborn>=0.13             # correlation heatmaps / bivariate plots

# --- Experiment tracking (Phase B) ---
google-cloud-aiplatform>=1.71.0   # Vertex AI Experiments SDK
google-cloud-storage>=2.18.0      # write artifacts (test scores, eval CSV) to GCS
