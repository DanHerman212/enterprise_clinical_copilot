# Runtime dependencies for the agent container.
# Ground-up LangChain-native stack (2026-08-23): the agent drives Gemini via
# ChatGoogleGenerativeAI (langchain-google-genai, Vertex backend) and wraps MCP
# tools as LangChain BaseTools, so Langfuse v4's official CallbackHandler
# captures everything natively (no hand-rolled @observe tracing).
#
# NOT langchain-mcp-adapters: declares mcp<2.0.0,>=1.24.0 and imports
# mcp.server.fastmcp (removed in 2.0) — incompatible with our mcp==2.0.0. We
# wrap MCPToolbox in our own BaseTool subclasses instead.
# NOT langchain-google-vertexai: ChatVertexAI is deprecated (removed in 4.0.0).
# Use langchain-google-genai.ChatGoogleGenerativeAI (Vertex backend) instead.

mcp==2.0.0
langgraph==1.2.11
langchain==1.3.16
langchain-core==1.6.0
langchain-google-genai==4.3.5
google-auth==2.50.0
uvicorn==0.47.0
langfuse==4.14.4
