"""Reusable production code for the readmission pipeline.

Modules:
  config    -- project/region/table FQNs, MIMIC version, split contract.
  tracking  -- Vertex AI Experiments wrapper (init, log_run, artifact upload).
"""
